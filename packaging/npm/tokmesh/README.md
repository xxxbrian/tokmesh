# tokmesh

npm wrapper for the [tokmesh](https://github.com/xxxbrian/tokmesh) CLI.

```sh
npm install -g tokmesh
tokmesh --help
# or
npx tokmesh --help
```

The install pulls a prebuilt native binary for your platform (same artifacts as GitHub Releases).

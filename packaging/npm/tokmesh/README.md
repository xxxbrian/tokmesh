# tokmesh

CLI for [tokmesh](https://github.com/xxxbrian/tokmesh) — local AI coding token analytics.

```sh
npm install -g tokmesh
tokmesh --help
```

Or run without a global install: `npx tokmesh --help`.
